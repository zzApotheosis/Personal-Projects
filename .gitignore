# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.o
*.a
a.out

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Everything under "target" directories for Rust
target*

# A hidden directory called ".cache" which comes from some Neovim plugin (probably Coc or NERDTree)
.cache
