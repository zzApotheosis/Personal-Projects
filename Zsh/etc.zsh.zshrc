# Clear the terminal at the start of each session
clear

# Set global aliases
alias c=clear;clear
alias ls="ls --color=auto"
alias l="ls -la"
alias e="vim"
alias whereami="pwd" # Seems to be an appropriate complement to whoami

