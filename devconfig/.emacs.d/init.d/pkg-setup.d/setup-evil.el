(use-package evil
  :ensure t)

;; Default to Emacs keybindings and leave Evil mode disabled but have it installed and available
(setq evil-default-state 'emacs)
;;(evil-mode t)

(setq-default pkg-installed-evil-p t)
