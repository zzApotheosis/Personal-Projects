#include <stdlib.h>
#include <stdio.h>

#include "mylib.h" // Header file exists somewhere else

int main(int argc, char * argv[]) {
  // Define method variables
  int exit_code = 0;

  hello();

  // Done
  return exit_code;
}
